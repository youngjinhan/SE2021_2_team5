import React from "react";
import styles from "./Editor.module.scss";

export interface Props {
  productId: string;
}

export const ProductEditor: React.FC<Props> = (props: Props) => {
  return <div></div>;
};
