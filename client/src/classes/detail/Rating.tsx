import React from "react";
import styles from "./Rating.module.scss";

export interface Props {
  productId: string;
}

export const ProductRating: React.FC<Props> = (props: Props) => {
  return <div></div>;
};
